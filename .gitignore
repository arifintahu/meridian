# Dependencies
node_modules/

# Environment & secrets
.env
.env.*
!.env.example
.envrypt

# user-config.json is TRACKED — it holds only non-secret tuning config.
# Keep all secrets (wallet key, RPC URL, API keys, tokens) in .env only.

# Runtime state (position tracking, lessons — user-specific)
state.json
lessons.json
smart-wallets.json
pool-memory.json
token-blacklist.json
strategy-library.json
decision-log.json
hivemind-cache.json
signal-weights.json

# Logs
logs/

# OS
.DS_Store

# Dev context
context.md
CONTEXT.md
dev-blocklist.json
# Ignore docs/ except the evaluation audit trail (docs/evals/), which is tracked.
docs/superpowers/

# Discord listener queue
discord-signals.json

# Playwright MCP browser logs
.playwright-mcp/

# Claude Code runtime lock
.claude/scheduled_tasks.lock

experiment.sqlite*