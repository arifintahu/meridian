---
experiment_id: exp-d1cd9a82
label: stage-agent01
evaluated_at: 2026-07-01T08:45:58Z   # 2026-07-01 15:45:58 SEAST
status_at_eval: ended 2026-07-01 08:40 UTC (2 open positions remaining)
closed_positions: 91
commits:
  - (none) — read-only evaluation, no config or code change
---

# Evaluation — exp-d1cd9a82 (`stage-agent01`)

- **Experiment:** `exp-d1cd9a82` — label `stage-agent01`
- **Started:** 2026-06-29 09:47 UTC · **Status at eval:** ended 2026-07-01 08:40 UTC (~47h), 2 positions still open
- **Evaluated:** 2026-07-01 08:45 UTC (SEAST)
- **Data source:** remote experiment Postgres (`EXPERIMENT_POSTGRES_URL`) via `scripts/eval-query.js`
- **Screening events:** 93 deploy · 138 no_deploy · 25 skip

> **This is the validation run for commit `46dfd54`.** The prior `stage-agent01`
> run (`exp-8486c3b1`, 134 closes, **−$36.15**) was net-negative entirely from 8
> stop-losses; that eval tightened `stopLossPct −12→−10`, `inRangeDrawdownPct
> −10→−8`, `inRangeDrawdownWaitMinutes 30→15`. This run ran on that tuned config.

---

## 1. Performance (91 closed positions)

| Metric | Value |
|---|---|
| Win rate | 75.8% of all (75.8% excl. break-even) — 69 win / 9 loss / 13 flat |
| Avg PnL% | +0.26% |
| Median PnL% | +0.07% |
| **Total PnL** | **+$10.75** |
| Total fees earned | +$36.40 (avg $0.40) |
| Avg range efficiency | 65.5% |
| Avg hold | 64.1 min (median 40) |

Implied price/IL drag = PnL − fees = **−$25.65**, more than offset by fees →
net positive. Fee-positive strategy, single-sided `bid_ask`-below exposure.

### Before / after vs the prior run (same label, same strategy)

| Metric | exp-8486c3b1 (before tune) | exp-d1cd9a82 (this run) |
|---|--:|--:|
| Total PnL | −$36.15 | **+$10.75** |
| Total fees | +$36.93 | +$36.40 |
| Stop-loss closes | 8 (−$51.15) | **2 (−$9.12)** |
| Worst single loss | −22.24% | **−10.56%** |
| Win rate | 68.7% | 75.8% |

The `−10` stop capped the tail exactly as the prior eval predicted; the loss
bucket that sank exp-8486c3b1 is gone.

### Exit-reason breakdown

| Reason | n | Avg PnL% | Total fees |
|---|--:|--:|--:|
| Out of range | 33 | +0.18% | $2.73 |
| Pumped far above range | 28 | +0.41% | $5.23 |
| Low yield | 17 | −0.33% | $1.12 |
| Trailing TP | 7 | +1.62% | $13.54 |
| Take profit | 4 | +5.09% | $9.70 |
| Stop loss | 2 | −10.08% | $4.08 |

**Profit concentration:** the 11 take-profit + trailing-TP closes = **+$14.39 PnL,
$23.24 fees (64% of all fees)**. The other 80 closes = **−$3.64 PnL, $13.16 fees**.
The edge is entirely the long-held winners; everything else churns roughly
break-even-plus-fees.

**Churn is structural:** 61/91 (67%) exit because price left the single-sided
below-range (OOR + pumped). The 12 sub-15m holds are noise (avg reff 41%, $0.31
total fees).

### Best 3 / Worst 3

- ✅ CHANCE-SOL +5.29% (take profit, 377m) · ALON-SOL +5.04% (take profit, 463m) · Potato-SOL +5.02% (take profit, 117m)
- ❌ Potato-SOL −10.56% (stop loss, reff 100) · Chameleon-SOL −9.60% (stop loss, reff 100, 390m) · BULLWIF-SOL −3.15% (low yield)

### Signal correlation (winners vs losers; loser = pnl ≤ 0)

| Signal | Winners | Losers | Read |
|---|--:|--:|---|
| organic_score | 74.7 | 75.6 | no signal |
| fee_tvl_ratio | 0.272 | 0.351 | weak (losers higher — chasing yield/vol) |
| volatility | 4.89 | 5.23 | no signal |
| entry_mcap | $1.66M | $1.58M | no signal |
| entry_tvl | $31.5k | $28.8k | no signal |
| entry_holders | 6,995 | 4,892 | weak (winners higher) |
| smart_wallets_present | 14.5% | 4.5% | weak (winners higher, tiny n) |

Bucket traps checked and rejected: org [75,80) shows 84% win / +0.80% avg, but
org ≥80 being net-negative (−$3.49) is **one token** — Potato-SOL's single −$4.78
stop-loss; remove it and org ≥80 is break-even. And org [60,70) — the bucket a
`minOrganic` raise would delete — earns **$10.26 in fees** for +$0.65 PnL. No
clean, real screening cut exists (same conclusion as exp-8486c3b1).

---

## 2. Diagnosis

The prior tune is **validated**: the run flipped from −$36.15 to +$10.75, stop-losses
dropped 8→2, and the worst loss was capped at −10.56% (was −22.24%). The strategy is
fee-positive — fees (+$36.40) outrun price/IL drag (−$25.65) — with profit concentrated
in ~11 long-held take-profit/trailing closes; the remaining 80 positions churn near
break-even as price leaves the single-sided below-range (67% OOR/pumped exits). No
screening signal separates winners from losers across either run, so thresholds stay
untouched. The one residual: the in-range drawdown rule still fired **0×** — both
stop-losses rode straight to −10 without the −8/15m soft cull engaging, and Chameleon
(low-vol 1.78, 390-min, reff 100) clearly dwelt in the −8…−10 band long enough to fire.
That is now low-impact (the stop caps the tail) but points at a possible drawdown-clock
engagement issue worth a separate code look — not a config lever.

---

## 3. Config changes applied (commit `—`)

**None — recommendations below were not applied.**

The run is profitable with a controlled tail; the prior tune (`46dfd54`) is validated.
Changing thresholds now would risk regressing a working config, and no signal in this
data supports a specific change.

**Considered and explicitly held:**

| Candidate | Verdict | Why held |
|---|---|---|
| Raise `minOrganic` 60→70 | ❌ reject | org [60,70) earns $10.26 fees for +$0.65 PnL — cutting it deletes real fee income to avoid break-even churn. |
| Add a `maxOrganic` cap (~80) | ❌ reject | org ≥80 "negative" is one token (Potato-SOL stop-loss); break-even without it. No `maxOrganic` key exists anyway. |
| Cap `fee_tvl_ratio` | ❌ reject | Losers higher in both runs but no clean cut-point, and high fee/TVL includes the big winners (Speedrun, CS). Would need a new config key (code change). |
| Tighten drawdown/stop further | ❌ reject | Tail already capped at −10.56%; more aggressive exits risk culling recoverable positions and adding churn. |

---

## 4. Code changes (commit `—`)

**None — read-only evaluation.**

Watch item for a future dedicated experiment (not this eval): the in-range drawdown
exit has now fired 0× across two consecutive `stage-agent01` runs. The prior follow-up
(`d86affe`) concluded the `−8`/`−10` band fixes the structural starvation; this run
shows the tail is capped but the soft rule still never engages — including on a low-vol
390-min bleed that should have triggered it. Worth tracing `state.js#updatePnlAndCheckExits`
drawdown-clock engagement (does `in_range_drawdown_since` reset on minor in-range
bounces above `−8`?) before assuming the parametric fix is complete.
