---
experiment_id: exp-5300018c
label: stage-agent01
evaluated_at: 2026-06-19T06:57:59Z   # 2026-06-19 13:57 SEAST
status_at_eval: running (3 open positions)
closed_positions: 38
commits:
  - b176346  feat(evaluate): read experiment data from Postgres, not local SQLite
  - f4351b8  tune: stopLossPct -50->-35, outOfRangeWaitMinutes 30->15
---

# Evaluation — exp-5300018c (`stage-agent01`)

- **Experiment:** `exp-5300018c` — label `stage-agent01`
- **Started:** 2026-06-18 06:23 UTC · **Status at eval:** still running (3 open positions)
- **Evaluated:** 2026-06-19 06:57 UTC (13:57 SEAST)
- **Data source:** remote experiment Postgres (`EXPERIMENT_POSTGRES_URL`) via `scripts/eval-query.js`
- **Screening events:** 41 deploy · 39 no_deploy · 28 skip

---

## 1. Performance (38 closed positions)

| Metric | Value |
|---|---|
| Win rate | 65.8% of all (80.6% excl. break-even) — 25 win / 6 loss / 7 flat |
| Avg PnL% | **−3.81%** |
| Median PnL% | +0.04% |
| **Total PnL** | **−$61.01** |
| Total fees earned | $15.94 |
| Avg range efficiency | 60.4% |
| Avg hold | 85 min |

**Headline:** a winning hit-rate but a losing book. 33 of 38 positions net roughly
flat-to-small-positive on fees; the entire −$61 comes from a 6-position downside tail.

### Exit-reason breakdown

| Reason | n | Avg PnL% |
|---|---|---|
| Pumped far above range | 16 | +0.34% |
| **Out of range** | **11** | **−8.60%** |
| Low yield | 6 | +0.14% |
| Trailing TP | 4 | +1.84% |
| Stop loss | 1 | −63.77% |

### Best 3 / Worst 3

- ✅ FLKR-SOL +2.87% (pumped) · Chaton-SOL +2.48% (trailing TP) · Islands-SOL +2.02% (trailing TP)
- ❌ **RIV-SOL −63.77%** (stop loss, −$26.65) · SELLOR-SOL −26.95% (OOR) · Vort-SOL −24.57% (OOR)

### Signal correlation (winners vs losers)

| Signal | Winners | Losers | Read |
|---|---|---|---|
| organic_score | 75.8 | 76.0 | no signal |
| fee_tvl_ratio | 0.37 | 0.32 | weak |
| volatility | 4.51 | 2.87 | inverted / noisy |
| entry_mcap | $1.22M | $689K | losers skew smaller |
| entry_tvl | $44.6K | $29.2K | losers skew smaller |
| entry_holders | 2320 | 1638 | losers skew smaller |

---

## 2. Diagnosis

This is a **tail-risk problem, not a selection problem.** The strategy is
single-sided `bid_ask` below the active bin: when price pumps *up*, the position
sits in quote and exits flat (the 16 "pumped" closes, +0.34%) — harmless. When
price breaks *down*, the position converts fully into the dumping base token and
bleeds. All 5 large losses are downside breaks held to −21%…−64% before exit.
`stopLossPct −50` and `outOfRangeWaitMinutes 30` let those losers run too long.

Entry signals barely separate winners from losers — the mcap/TVL/holders skew is
real but mild and cuts genuine winners too (e.g. 滑る猫 +1.38% at $198K mcap), so
screening thresholds were left unchanged.

---

## 3. Config changes applied (commit `f4351b8`)

Edited `user-config.json` (git-tracked; runner picks up via `git pull`). Affects
future deploys only — the run was still in progress at eval time.

| Key | Before | After | Rationale |
|---|---|---|---|
| `stopLossPct` | `-50` | `-35` | The single −63.77% loss is ~44% of all losses. A −35% cap bites only clearly-failing positions; no winner ever went near −35%. |
| `outOfRangeWaitMinutes` | `30` | `15` | The 5 big OOR losers bled 99–166 min while waiting. Halving the downside-break wait cuts IL accumulation; upside breaks are handled separately by the "pumped far above range" rule. |

```json
// before → after
{
  "stopLossPct": -50,            // → -35
  "outOfRangeWaitMinutes": 30    // → 15
}
```

**Not changed (mixed evidence):** `minMcap` / `minHolders` / `minTvl`. Averages
skew toward larger tokens for winners, but several real winners are small-cap, so
tightening would discard income too.

---

## 4. Code change — `/evaluate` now reads from Postgres (commit `b176346`)

**Why:** `exp-5300018c` could not be found because `/evaluate` queried the local
SQLite (`db/connection.js`). Experiments from other runners only exist in the
remote Postgres (`EXPERIMENT_POSTGRES_URL`) that the outbox worker (`db/sync.js`)
syncs to. The daemon writes to local SQLite; Postgres is the cross-runner source
of truth.

### 4a. New file: `scripts/eval-query.js`

Postgres-backed `list` / `load <label-or-id>` using the existing `getPool()`.
Normalises Postgres types back to the SQLite shape the analysis expects:
BIGINT/NUMERIC → JS numbers, JSONB → JSON strings (so downstream `JSON.parse`
keeps working). Errors clearly if `EXPERIMENT_POSTGRES_URL` is unset.

```js
import 'dotenv/config';
import { getPool, closePool } from '../db/postgres.js';

const NUMERIC_COLS = new Set([
  'started_at','ended_at','ts','deployed_at','closed_at','amount_sol','bin_step',
  'active_bin_at_deploy','volatility','fee_tvl_ratio','organic_score','entry_mcap',
  'entry_tvl','entry_volume','entry_holders','minutes_held','minutes_in_range',
  'range_efficiency','fees_earned_usd','pnl_usd','pnl_pct','n',
]);
const JSON_COLS = new Set([
  'config_snapshot','signal_snapshot','bin_range','risks','metrics','rejected',
]);

function normalizeRow(row) {
  const out = {};
  for (const [k, v] of Object.entries(row)) {
    if (v === null || v === undefined) { out[k] = v; continue; }
    if (NUMERIC_COLS.has(k)) { out[k] = Number(v); continue; }
    if (JSON_COLS.has(k)) { out[k] = typeof v === 'object' ? JSON.stringify(v) : v; continue; }
    out[k] = v;
  }
  return out;
}
// list() / load() query Postgres via getPool(); see scripts/eval-query.js for full source.
```

### 4b. `.claude/commands/evaluate.md` — before / after

Step 1 (find experiments):

```diff
-node -e "
-import('./db/connection.js').then(({ initDb }) => {
-  const db = initDb();
-  const rows = db.prepare('SELECT id, label, started_at, ended_at FROM experiments ORDER BY started_at DESC LIMIT 10').all();
-  for (const r of rows) {
-    const started = new Date(r.started_at).toISOString().slice(0,16);
-    const ended = r.ended_at ? new Date(r.ended_at).toISOString().slice(0,16) : 'running';
-    console.log(r.id, '|', r.label, '|', started, '->', ended);
-  }
-  db.close();
-});
-"
+node scripts/eval-query.js list
```

Step 2 (load positions + screening events):

```diff
-node -e "
-const LABEL = 'LABEL_OR_ID';
-import('./db/connection.js').then(({ initDb }) => {
-  const db = initDb();
-  const exp = db.prepare('SELECT * FROM experiments WHERE label = ? OR id = ? ORDER BY started_at DESC LIMIT 1').get(LABEL, LABEL);
-  if (!exp) { console.log('not found'); db.close(); return; }
-  const positions = db.prepare('SELECT * FROM positions WHERE experiment_id = ? ORDER BY deployed_at ASC').all(exp.id);
-  const closed = positions.filter(p => p.closed_at);
-  const screening = db.prepare('SELECT type, COUNT(*) as n FROM screening_events WHERE experiment_id = ? GROUP BY type').all(exp.id);
-  console.log('=== EXPERIMENT ===');      console.log(JSON.stringify(exp, null, 2));
-  console.log('=== SCREENING ===');       console.log(JSON.stringify(screening, null, 2));
-  console.log('=== CLOSED POSITIONS ==='); console.log(JSON.stringify(closed, null, 2));
-  console.log('=== OPEN POSITIONS ===');  console.log(JSON.stringify(positions.filter(p => !p.closed_at), null, 2));
-  db.close();
-});
-"
+node scripts/eval-query.js load LABEL_OR_ID
```

Front-matter `description` and a **Data source** note were also updated to point
at Postgres instead of SQLite.
